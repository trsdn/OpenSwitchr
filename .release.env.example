# Signing identity used by scripts/build-app.sh
# Either a certificate SHA-1 fingerprint or a substring of the identity name.
OPENSWITCH_SIGNING_IDENTITY=

# Notarization: either a stored notarytool profile...
NOTARY_PROFILE=OpenSwitch

# ...or explicit credentials.
APPLE_ID=
APPLE_TEAM_ID=
APPLE_APP_PASSWORD=
